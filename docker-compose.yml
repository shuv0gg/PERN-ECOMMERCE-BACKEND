services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: shuvo234778
      POSTGRES_DB: EcommerceDB
    volumes:
      - db_data:/var/lib/postgresql/data

  pg_admin:
    image: dpage/pgadmin4
    ports:
      - "8080:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: mek234778@gmail.com
      PGADMIN_DEFAULT_PASSWORD: testingpassword
    depends_on:
      - db

volumes:
  db_data:
